[warn] Chroma unavailable: No module named 'chromadb'
== 1. Ingest personal memories ==
  stored #1: Meeting with Artem: AI router deadline is Friday (tags=['work', 'meetings'])
  stored #2: Pasta recipe: 200g pasta, 100g cheese, one egg (tags=['cooking'])
  stored #3: Office address: Moscow, Lenina street 10 (tags=['work'])
  stored #4: Book idea: local-first AI memory as a second brain (tags=['ideas'])

== 2. Search (top_k=2) ==
  Q: 'when is the deadline'
    -> #1 score=1: Meeting with Artem: AI router deadline is Friday
  Q: 'how to cook pasta'
    -> #2 score=2: Pasta recipe: 200g pasta, 100g cheese, one egg

== 3. Export full memory to portable JSON ==
  format=knowledge-os-export version=1 episodes=4 -> export_before_delete.json

== 4. Permanently delete one memory ==
  delete(#1) -> True
  delete(#1) again -> False (idempotent, already gone)
  episodes after delete: 3 (was 4)
  verified: deleted episode absent from export AND from search results

Demo store was temporary; nothing persists on disk except the JSON export above.
