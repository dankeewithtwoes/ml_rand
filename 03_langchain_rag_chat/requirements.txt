# Offline core (grounding.py, tests/test_grounding.py, examples/grounding_demo.py):
# standard library only — `pip install` is NOT required for the demo or the tests.

# Pipeline runtime, pinned to the versions installed in the dev environment.
# Needed only for rag_chat.py / evaluate_rag.py / build_index.py.
python-dotenv==1.2.2
numpy==2.1.2
scikit-learn==1.8.0
langchain==0.3.28
langchain-community==0.4.2
langchain-openai==1.1.12

# Heavy / optional: required only for the full hybrid pipeline, NOT installed in
# the verified offline environment. sentence-transformers pulls in torch;
# chromadb ships its own embedded database. Install on demand:
rank-bm25>=0.2.2          # BM25 half of the EnsembleRetriever
sentence-transformers>=2.7.0  # embeddings + cross-encoder reranker (pulls torch)
chromadb>=0.5.0           # persistent vector store
